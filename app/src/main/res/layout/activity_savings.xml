<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2026 AtaraxiaGoddess. All rights reserved.
  -->

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Top spacer to account for the month selector from BaseActivity -->
            <View
                android:layout_width="match_parent"
                android:layout_height="@dimen/home_content_top_inset"
                android:visibility="invisible" />

            <!-- ===== Available Pool Card (always visible) ===== -->
            <LinearLayout
                android:id="@+id/availablePoolCard"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/category_background"
                android:padding="12dp"
                android:elevation="2dp"
                android:layout_marginBottom="16dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    style="@style/TextAppearance.BudgetBrewer.Body"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/available_pool_label"
                    android:textColor="@color/text_on_container" />

                <Space
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="1dp" />

                <TextView
                    android:id="@+id/tvAvailablePoolAmount"
                    style="@style/TextAppearance.BudgetBrewer.Body"
                    android:textStyle="bold"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_on_container" />
            </LinearLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <!-- Empty state (visible when no buckets) -->
                <include
                    android:id="@+id/emptyStateContainer"
                    layout="@layout/savings_empty_state"
                    android:visibility="gone" />

                <!-- Bucket list (RecyclerView) -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewBuckets"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clipToPadding="false"
                    android:padding="4dp"
                    android:visibility="gone" />
            </FrameLayout>
            <!-- Bottom spacer (keeps content above bottom nav) -->
            <View
                android:layout_width="match_parent"
                android:layout_height="@dimen/home_content_bottom_inset"
                android:layout_gravity="bottom"
                android:visibility="invisible" />
        </LinearLayout>
    </ScrollView>

    <!-- FAB for adding buckets -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddBucket"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="50dp"
        android:layout_marginBottom="100dp"
        android:contentDescription="@string/add_bucket"
        android:src="@drawable/ic_add"
        app:fabSize="normal"
        app:tint="@android:color/white"
        tools:ignore="ImageContrastCheck,ClassNameCheck" />

    <!-- ProgressBar overlay (matches existing pattern) -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />
</FrameLayout>
